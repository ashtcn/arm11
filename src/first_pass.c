#include "string_array.h"
#include "symbol_table.h"
#include "symbol_table_toolbox.c"

symbol_table_t *generate_symbol_table(string_array_t **tokens) {
  symbol_table_t *table = create_table(100);
  address_t address = 0;

  for () {
    if () {
      address += 4;
    } else {
      add_row(table, , address)
    }
  }

  return table;
}
